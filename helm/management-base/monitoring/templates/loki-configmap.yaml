{{- if .Values.loki.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-config
  namespace: {{ include "monitoring.namespace" . }}
  labels:
    {{- include "monitoring.loki.labels" . | nindent 4 }}
data:
  loki.yaml: |
    auth_enabled: false

    server:
      http_listen_port: 3100
      grpc_listen_port: 9096

    common:
      path_prefix: /loki
      replication_factor: 1
      ring:
        instance_addr: 127.0.0.1
        kvstore:
          store: inmemory

    schema_config:
      configs:
        - from: 2024-01-01
          store: boltdb-shipper
          {{- if .Values.loki.s3.enabled }}
          object_store: s3
          {{- else }}
          object_store: filesystem
          {{- end }}
          schema: v11
          index:
            prefix: index_
            period: 24h

    storage_config:
      {{- if .Values.loki.s3.enabled }}
      aws:
        s3: s3://{{ .Values.loki.s3.region }}/{{ .Values.loki.s3.bucketName }}
        bucketnames: {{ .Values.loki.s3.bucketName }}
        region: {{ .Values.loki.s3.region }}
        s3forcepathstyle: false
      {{- end }}
      boltdb_shipper:
        active_index_directory: /loki/index
        cache_location: /loki/cache
        {{- if .Values.loki.s3.enabled }}
        shared_store: s3
        {{- else }}
        shared_store: filesystem
      filesystem:
        directory: /loki/chunks
        {{- end }}

    {{- if .Values.loki.retention.enabled }}
    limits_config:
      retention_period: {{ .Values.loki.retention.period }}
      max_streams_per_user: {{ .Values.loki.limits.maxStreams }}
      ingestion_rate_mb: {{ .Values.loki.limits.ingestionRateMb }}
      ingestion_burst_size_mb: {{ .Values.loki.limits.ingestionBurstSizeMb }}
    {{- end }}

    compactor:
      working_directory: /loki/compactor
      {{- if .Values.loki.s3.enabled }}
      shared_store: s3
      {{- else }}
      shared_store: filesystem
      {{- end }}
      compaction_interval: 10m
      {{- if .Values.loki.retention.enabled }}
      retention_enabled: true
      retention_delete_delay: 2h
      retention_delete_worker_count: 150
      {{- end }}

    ruler:
      storage:
        type: local
        local:
          directory: /loki/rules
      rule_path: /loki/rules-temp
      alertmanager_url: http://localhost:9093
      ring:
        kvstore:
          store: inmemory
      enable_api: true
{{- end }}

