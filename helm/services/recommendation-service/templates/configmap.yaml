apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "recommendation-service.fullname" . }}-config
  labels:
    {{- include "recommendation-service.labels" . | nindent 4 }}
data:
  application.yaml: |
    # Application configuration
    # Add any application-specific configuration here

