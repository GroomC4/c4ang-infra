{{- if .Values.security.jwt.enabled }}
# ====================================
# Gateway-level JWT Authentication
# ====================================
# Gateway Only: JWT 검증은 Gateway에서만 수행
# 서비스는 X-User-Id, X-User-Role 헤더만 신뢰
# 서비스 간 통신은 mTLS로 신뢰
#
# NOTE: STRICT mTLS 환경에서 istiod가 jwksUri를 fetch할 수 없으므로
# inline jwks를 사용합니다. 키 로테이션 시 이 값도 업데이트해야 합니다.
apiVersion: security.istio.io/v1
kind: RequestAuthentication
metadata:
  name: gateway-jwt-auth
  namespace: {{ include "istio.namespace" . }}
  labels:
    {{- include "istio.labels" . | nindent 4 }}
spec:
  selector:
    matchLabels:
      # Gateway API에서 생성된 Pod의 실제 레이블
      gateway.networking.k8s.io/gateway-name: {{ include "istio.gateway.main.name" . }}
  jwtRules:
    - issuer: "{{ .Values.security.jwt.issuer }}"
      {{- if .Values.security.jwt.jwks }}
      # Inline JWKS (STRICT mTLS 환경에서 권장)
      jwks: {{ .Values.security.jwt.jwks | quote }}
      {{- else }}
      # jwksUri 사용 (PERMISSIVE mTLS 또는 네트워크 접근 가능한 경우)
      jwksUri: "{{ .Values.security.jwt.jwksUri }}"
      {{- end }}
      {{- if .Values.security.jwt.audiences }}
      audiences:
        {{- range .Values.security.jwt.audiences }}
        - {{ . | quote }}
        {{- end }}
      {{- end }}
      forwardOriginalToken: true
      outputClaimToHeaders:
        - header: "X-User-Id"
          claim: "sub"
        - header: "X-User-Role"
          claim: "role"
        - header: "X-User-Email"
          claim: "email"
        - header: "X-User-Name"
          claim: "name"
{{- end }}
