# ArgoCD ApplicationSet - MSA Services (Dev)
# k3d 로컬 클러스터용
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: services
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - list:
        elements:
          - name: customer-service
          # Discord 알림 테스트를 위해 임시로 다른 서비스 비활성화
          # - name: order-service
          # - name: payment-service
          # - name: product-service
          # - name: saga-tracker
          # - name: store-service

  template:
    metadata:
      name: '{{.name}}'
      namespace: argocd
      labels:
        app.kubernetes.io/component: '{{.name}}'
        app.kubernetes.io/env: dev
        app.kubernetes.io/type: service
      annotations:
        argocd.argoproj.io/sync-wave: "30"
    spec:
      project: applications
      sources:
        - repoURL: https://github.com/GroomC4/c4ang-infra.git
          targetRevision: feature/argocd-analysis-template-enhancement
          ref: values
        - repoURL: https://github.com/GroomC4/c4ang-infra.git
          targetRevision: feature/argocd-analysis-template-enhancement
          path: 'charts/services/{{.name}}'
          helm:
            valueFiles:
              - values.yaml
              - '$values/config/dev/{{.name}}.yaml'
      destination:
        server: https://kubernetes.default.svc
        namespace: ecommerce
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 5
          backoff:
            duration: 10s
            factor: 2
            maxDuration: 5m
