apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "store-service.fullname" . }}-config
  labels:
    {{- include "store-service.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
