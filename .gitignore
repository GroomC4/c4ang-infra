# intelij
.idea

# Helm dependencies (서브차트)
**/charts/*.tgz
*.tgz
Chart.lock

# Temporary files
tmp/
temp/
*.tmp
*.swp
*~

# OS files
.DS_Store
Thumbs.db

# Local development files
**/values-*.local.yaml
**/values.local.yaml
*.local.yaml
helm/services/customer-service/apiVersion: v1.yaml

# SOPS 평문 시크릿 파일 (암호화된 .enc.yaml만 Git에 커밋)
*.secrets.yaml
**/values.secrets.yaml
**/*.secrets.yaml
config/dev/*.secrets.yaml
config/prod/*.secrets.yaml

# Istio 바이너리 (개발 환경에만 존재)
environments/dev/istio/

# EKS 관련 파일 (프로덕션 환경 설정) - 레거시
# k8s-eks/  # config/prod/로 이동됨

# 프로덕션 시크릿 (config/prod에서 시크릿 파일 제외)
config/prod/*.secrets.yaml

# Istio 바이너리 및 다운로드 파일
**/istio-*/
**/istioctl

# 테스트 및 임시 파일
*.test.sh
*.local.sh
TEST.md
TEST*.md
*.log
*.out

# 포트포워딩 PID 파일
.port-forward/
scripts/platform/kubeconfig.yaml

# K3d kubeconfig (로컬 개발환경 - Git에 커밋하지 않음)
k8s-dev-k3d/kubeconfig/
