{{- if .Values.istio.enabled }}
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: {{ include "store-service.fullname" . }}-vs
  labels:
    {{- include "store-service.labels" . | nindent 4 }}
spec:
  hosts:
    - {{ include "store-service.fullname" . }}
  http:
    - match:
        - uri:
            prefix: {{ .Values.istio.pathPrefix | default "/api/v1/stores" }}
      route:
        - destination:
            host: {{ include "store-service.fullname" . }}
            port:
              number: {{ .Values.service.port }}
      timeout: {{ .Values.istio.timeout | default "30s" }}
      retries:
        attempts: {{ .Values.istio.retries.attempts | default 3 }}
        perTryTimeout: {{ .Values.istio.retries.perTryTimeout | default "10s" }}
        retryOn: {{ .Values.istio.retries.retryOn | default "5xx,reset,connect-failure,refused-stream" }}
{{- end }}

