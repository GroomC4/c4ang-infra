# Infracost GitHub App 설정 가이드

GitHub App은 Infracost를 GitHub 리포지토리에 통합하는 가장 간단하고 권장되는 방법입니다. CI/CD 파이프라인 설정 없이 자동으로 Pull Request에 비용 정보를 표시합니다.

## 📋 목차

1. [GitHub App이란?](#github-app이란)
2. [장점](#장점)
3. [설정 방법](#설정-방법)
4. [고급 기능](#고급-기능)
5. [문제 해결](#문제-해결)

## GitHub App이란?

Infracost GitHub App은 GitHub에서 검증된 앱으로, 수천 개의 GitHub 조직에 설치되어 있습니다. Pull Request에 자동으로 비용 추정, 태깅, FinOps 정책 이슈를 코멘트로 추가합니다.

## 장점

GitHub App을 사용하면 다음과 같은 이점이 있습니다:

1. **CI/CD 설정 불필요**
   - GitHub Actions나 다른 CI/CD 도구 설정이 필요 없습니다
   - Infracost가 자동으로 모든 것을 관리합니다

2. **한 번의 클릭으로 여러 리포지토리 추가**
   - 여러 리포지토리에 한 번에 추가할 수 있습니다
   - CLI 버전을 설치하거나 업데이트할 필요가 없습니다

3. **AutoFix PR 자동 생성**
   - 기존 이슈에 대한 자동 수정 Pull Request를 생성합니다
   - 엔지니어가 검토하고 병합할 수 있습니다

4. **더 빠른 실행**
   - 변경된 폴더만 실행됩니다
   - GitHub App 이벤트를 기반으로 작동합니다

5. **자동 업데이트**
   - Infracost가 최신 버전을 유지합니다
   - 수동 업데이트가 필요 없습니다

## 설정 방법

### 1. Infracost Cloud 가입

1. **Infracost Cloud 접속**
   - https://www.infracost.io/cloud 접속
   - "Get Started" 클릭

2. **계정 생성**
   - GitHub 계정으로 로그인 (권장)
   - 또는 이메일로 가입
   - GitHub 로그인 문제가 있으면 [FAQ](https://www.infracost.io/docs/faq/) 참조

3. **무료 체험 시작**
   - 신용카드 불필요
   - 즉시 사용 가능

### 2. 조직 생성

1. **기본 조직**
   - 모든 사용자는 개인용 기본 조직을 가지고 있습니다

2. **회사 조직 생성**
   - 페이지 상단의 조직 드롭다운 사용
   - "Create new organization" 클릭
   - 조직 이름 입력

### 3. GitHub App 설치

1. **설정 페이지 이동**
   - Settings > Org Settings > Integrations > GitHub 이동

2. **리포지토리 선택**
   - 마법사를 따라 진행
   - Infracost에게 접근 권한을 부여할 리포지토리 선택
   - 전체 조직 또는 특정 리포지토리 선택 가능

3. **권한 부여**
   - GitHub에서 권한 확인
   - 필요한 권한:
     - **읽기 전용**: Metadata, Contents, Issues, Pull requests 등
     - **읽기/쓰기**: Pull requests (코멘트 작성), Contents (AutoFix PR 생성)

### 4. 설정 파일 추가 (선택사항)

Infracost가 코드를 분석하는 방법을 커스터마이즈할 수 있습니다:

1. **리포지토리 루트에 설정 파일 추가**
   - `infracost.yml` 또는 `infracost.yml.tmpl` 파일 생성
   - 또는 Repo > my repo > Settings 탭에서 설정

2. **사용량 파일 추가**
   - `infracost-usage.yml` 파일로 사용량 값 정의
   - 리소스별 사용량 지정 가능

예제 `infracost.yml`:

```yaml
version: 0.1

terraform_dir: .
terraform_var_files:
  - only-rds.tfvars
```

### 5. 테스트

1. **테스트 Pull Request 생성**
   - 리포지토리에 테스트 Pull Request 생성
   - Terraform 코드 변경 포함

2. **자동 코멘트 확인**
   - Infracost가 자동으로 코멘트 추가
   - 비용 추정, 태깅 정책, FinOps 정책 이슈 표시

3. **대시보드 확인**
   - Infracost Cloud 대시보드에서도 비용 추정 확인
   - Pull Request가 병합되면 대시보드에 표시

## 고급 기능

### Pull Request 코멘트 비활성화

비용 추정을 Infracost Cloud 대시보드에서만 확인하고 싶다면:

1. **설정 페이지 이동**
   - Org Settings > Integrations > GitHub App 페이지

2. **코멘트 비활성화**
   - "Disable pull request comments" 옵션 활성화
   - 엔지니어 워크플로우에 영향을 주지 않고 테스트 가능

### 정책 이슈 해제 또는 일시 중지

엔지니어가 중요한 변경사항을 빠르게 배포할 수 있도록:

1. **GitHub Pull Request UI에서 직접 해제/일시 중지**
   - 정책에서 수정을 요구하더라도 가능
   - 이슈 해제 또는 일시 중지 가능

2. **도움말 확인**
   - Pull Request 코멘트에 `@infracost help` 추가
   - 해제/일시 중지 방법 확인

### Required Check로 설정

정책을 "엔지니어가 이슈 해결 또는 해제 필요"로 표시하면, Infracost는 해당 정책을 실패한 Pull Request에 대해 "failed" 상태 체크를 GitHub에 반환합니다.

그러나 이것만으로는 GitHub에서 병합 버튼이 차단되지 않습니다. 병합을 차단하려면 Infracost를 "Required Check"로 표시해야 합니다:

#### 방법 1: GitHub Ruleset 사용 (권장)

1. **GitHub에서 Ruleset 생성**
   - Settings > Repository > Rulesets 이동
   - "New ruleset" 클릭

2. **규칙 설정**
   - 이름: "Infracost Required Check"
   - 적용 범위: 모든 리포지토리 또는 특정 리포지토리

3. **Required Check 추가**
   - "Required status checks" 섹션 추가
   - 체크 이름: "Infracost" (대문자 I 사용)
   - "Require branches to be up to date" 옵션 선택 (선택사항)

4. **적용**
   - 조직의 모든 리포지토리에 적용
   - 또는 특정 리포지토리에만 적용

#### 방법 2: Branch Protection Rules 사용

1. **리포지토리별 설정**
   - Settings > Branches > Protect matching branches

2. **규칙 설정**
   - 브랜치 패턴: `main` 또는 `master`
   - "Require status checks to pass before merging" 옵션 활성화

3. **Required Check 추가**
   - "Infracost" 체크 추가
   - "Require branches to be up to date" 옵션 선택 (선택사항)

**참고**: Branch Protection Rules는 리포지토리별로 설정해야 하므로, 여러 리포지토리에 적용하려면 GitHub API를 사용하여 자동화해야 합니다.

### AutoFix PR 기능

Infracost는 기존 이슈에 대한 자동 수정 Pull Request를 생성할 수 있습니다:

1. **이슈 탐색기에서 이슈 확인**
   - Visibility > Issue Explorer 이동
   - FinOps 정책 이슈 확인

2. **AutoFix PR 생성**
   - 이슈에서 "Open AutoFix PR" 버튼 클릭
   - 자동으로 수정 Pull Request 생성

3. **검토 및 병합**
   - 엔지니어가 검토
   - 승인 후 병합

**참고**: 태깅 정책은 AutoFix Pull Request에서 지원되지 않습니다.

## GitHub Enterprise 지원

### GitHub Enterprise Cloud

GitHub Enterprise Cloud에서는 일반 GitHub App과 동일한 방법으로 사용할 수 있습니다:

1. **동일한 설정 방법 사용**
   - 위의 설정 방법과 동일

2. **IP 허용 목록 설정 (필요한 경우)**
   - GitHub Enterprise "Enable IP allow list" 사용 시
   - Infracost GitHub App이 자동으로 필요한 IP 주소 추가
   - 또는 수동으로 다음 IP 주소 허용:
     - 3.133.40.66
     - 3.16.104.91
     - 3.147.121.170
     - 3.141.214.65
     - 18.221.82.195
     - 18.119.42.142

3. **아웃고잉 트래픽 허용 (필요한 경우)**
   - 인스턴스에서 아웃고잉 트래픽이 제한된 경우
   - `dashboard.api.infracost.io:443` 허용
   - 또는 IP 주소 허용: `52.223.24.69`, `76.223.127.201`

### GitHub Enterprise Server

GitHub Enterprise Server를 사용하는 경우:

1. **이메일로 문의**
   - support@infracost.io로 이메일 발송
   - GitHub Enterprise Server 활성화 요청

2. **서버 관리자와 회의**
   - Infracost GitHub App 설치를 위한 회의 필요
   - 조직에 앱 설치

3. **mTLS 지원 (선택사항)**
   - 클라이언트 인증서 필요
   - Infracost에 인증서 제공
   - 안전하게 저장 및 암호화됨

## GitHub Actions에서 GitHub App으로 마이그레이션

기존에 GitHub Actions를 사용하고 있다면 GitHub App으로 마이그레이션할 수 있습니다:

1. **GitHub App 설치**
   - 위의 설정 방법 참조
   - 동일한 Infracost 조직 사용 가능

2. **테스트**
   - 테스트 Pull Request 생성
   - GitHub App이 정상 작동하는지 확인

3. **GitHub Actions 제거**
   - GitHub Actions에서 모든 Infracost 단계 제거
   - `.github/workflows/infracost.yml` 파일 삭제 (또는 주석 처리)

4. **확인**
   - Pull Request에서 Infracost 코멘트 확인
   - GitHub App이 정상 작동하는지 확인

## 문제 해결

### GitHub App이 코멘트를 추가하지 않음

**원인**: 리포지토리에 Terraform 코드가 없거나, Pull Request에 Terraform 변경사항이 없음

**해결 방법**:
- Pull Request에 Terraform 코드 변경사항 포함 확인
- 리포지토리 루트에 `.tf` 파일이 있는지 확인

### 권한 오류

**원인**: GitHub App에 필요한 권한이 없음

**해결 방법**:
- Settings > Integrations > GitHub App에서 권한 확인
- 필요한 권한 부여 확인

### 설정 파일이 인식되지 않음

**원인**: 설정 파일 경로 또는 형식 오류

**해결 방법**:
- `infracost.yml` 파일이 리포지토리 루트에 있는지 확인
- YAML 형식이 올바른지 확인
- Repo > my repo > Settings 탭에서 설정 확인

### Required Check가 작동하지 않음

**원인**: GitHub Ruleset 또는 Branch Protection Rules 설정 오류

**해결 방법**:
- 체크 이름이 "Infracost" (대문자 I)인지 확인
- Ruleset이 올바른 브랜치에 적용되었는지 확인
- Branch Protection Rules가 활성화되었는지 확인

## 참고 자료

- [Infracost GitHub App 공식 문서](https://www.infracost.io/docs/integrations/github_app/)
- [Infracost Cloud 대시보드](https://www.infracost.io/cloud)
- [GitHub Rulesets 문서](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-rulesets/about-rulesets)
- [GitHub Branch Protection Rules 문서](https://docs.github.com/en/repositories/configuring-branches-and-merges-in-your-repository/managing-protected-branches/about-protected-branches)

## 요약

GitHub App은 Infracost를 GitHub에 통합하는 가장 간단하고 권장되는 방법입니다:

1. ✅ CI/CD 설정 불필요
2. ✅ 자동으로 Pull Request에 코멘트 추가
3. ✅ AutoFix PR 자동 생성
4. ✅ 변경된 폴더만 실행되어 더 빠름
5. ✅ 자동 업데이트

설정은 매우 간단합니다:
1. Infracost Cloud 가입
2. GitHub App 설치
3. 리포지토리 선택
4. 테스트 Pull Request 생성

더 자세한 정보는 [Infracost GitHub App 공식 문서](https://www.infracost.io/docs/integrations/github_app/)를 참고하세요.

