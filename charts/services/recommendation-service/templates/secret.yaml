{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "recommendation-service.fullname" . }}-secret
  labels:
    {{- include "recommendation-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
