{{- if .Values.secrets }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "store-service.fullname" . }}-secret
  labels:
    {{- include "store-service.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
{{- end }}
