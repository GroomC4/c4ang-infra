# k3d 개발 환경 Argo Rollouts 설정

namespace: argo-rollouts

argo-rollouts:
  controller:
    replicas: 1
    metrics:
      enabled: true
      serviceMonitor:
        enabled: false

  dashboard:
    enabled: true
    service:
      type: ClusterIP
      port: 3100

  installCRDs: true

  notifications:
    secret:
      create: true

# 개발 환경 모니터링 설정
metrics:
  enabled: true
  port: 8090
  targetPort: 8090

prometheus:
  enabled: true
  scrape: true
  path: /metrics
  port: 8090
