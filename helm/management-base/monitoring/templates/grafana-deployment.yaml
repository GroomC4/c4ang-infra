{{- if .Values.grafana.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
  namespace: {{ include "monitoring.namespace" . }}
  labels:
    {{- include "monitoring.grafana.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.grafana.replicas }}
  selector:
    matchLabels:
      {{- include "monitoring.grafana.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "monitoring.grafana.selectorLabels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/grafana-configmap.yaml") . | sha256sum }}
        checksum/secret: {{ include (print $.Template.BasePath "/grafana-secret.yaml") . | sha256sum }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      
      securityContext:
        fsGroup: 472
        runAsUser: 472
        runAsNonRoot: true
      
      containers:
        - name: grafana
          image: "{{ .Values.grafana.image.repository }}:{{ .Values.grafana.image.tag }}"
          imagePullPolicy: {{ .Values.grafana.image.pullPolicy }}
          
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          
          env:
            - name: GF_SECURITY_ADMIN_USER
              valueFrom:
                secretKeyRef:
                  name: {{ if .Values.grafana.admin.existingSecret }}{{ .Values.grafana.admin.existingSecret }}{{ else }}grafana-admin{{ end }}
                  key: {{ .Values.grafana.admin.secretKey }}
            - name: GF_SECURITY_ADMIN_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ if .Values.grafana.admin.existingSecret }}{{ .Values.grafana.admin.existingSecret }}{{ else }}grafana-admin{{ end }}
                  key: admin-password
            - name: GF_PATHS_DATA
              value: /var/lib/grafana
            - name: GF_PATHS_LOGS
              value: /var/log/grafana
            - name: GF_PATHS_PLUGINS
              value: /var/lib/grafana/plugins
            - name: GF_PATHS_PROVISIONING
              value: /etc/grafana/provisioning
          
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 30
            successThreshold: 1
            failureThreshold: 3
          
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
            timeoutSeconds: 30
            successThreshold: 1
            failureThreshold: 3
          
          resources:
            {{- toYaml .Values.grafana.resources | nindent 12 }}
          
          volumeMounts:
            - name: storage
              mountPath: /var/lib/grafana
            - name: datasources
              mountPath: /etc/grafana/provisioning/datasources
            {{- if .Values.grafana.dashboards.enabled }}
            - name: dashboards-provisioning
              mountPath: /etc/grafana/provisioning/dashboards
            - name: dashboard-ecommerce-overview
              mountPath: /var/lib/grafana/dashboards/ecommerce-overview.json
              subPath: ecommerce-overview.json
            - name: dashboard-ecommerce-service-detail
              mountPath: /var/lib/grafana/dashboards/ecommerce-service-detail.json
              subPath: ecommerce-service-detail.json
            - name: dashboard-ecommerce-resources
              mountPath: /var/lib/grafana/dashboards/ecommerce-resources.json
              subPath: ecommerce-resources.json
            - name: dashboard-ecommerce-logs-visual
              mountPath: /var/lib/grafana/dashboards/ecommerce-logs-visual.json
              subPath: ecommerce-logs-visual.json
            - name: dashboard-cache-redis
              mountPath: /var/lib/grafana/dashboards/cache-redis.json
              subPath: cache-redis.json
            {{- end }}
      
      volumes:
        - name: storage
          persistentVolumeClaim:
            claimName: grafana-storage
        - name: datasources
          configMap:
            name: grafana-datasources
        {{- if .Values.grafana.dashboards.enabled }}
        - name: dashboards-provisioning
          configMap:
            name: grafana-dashboards
        - name: dashboard-ecommerce-overview
          configMap:
            name: grafana-dashboard-ecommerce-overview
        - name: dashboard-ecommerce-service-detail
          configMap:
            name: grafana-dashboard-ecommerce-service-detail
        - name: dashboard-ecommerce-resources
          configMap:
            name: grafana-dashboard-ecommerce-resources
        - name: dashboard-ecommerce-logs-visual
          configMap:
            name: grafana-dashboard-ecommerce-logs-visual
        - name: dashboard-cache-redis
          configMap:
            name: grafana-dashboard-cache-redis
        {{- end }}
      
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}

